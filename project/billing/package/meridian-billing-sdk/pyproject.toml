[project]
name = "meridian-billing-sdk"
version = "0.1.0"
description = "Validate and preview invoice batches"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
meridian-invoices = "meridian_billing_sdk.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
