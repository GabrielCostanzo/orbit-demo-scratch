[project]
name = "meridian-config"
version = "0.1.0"
description = "Environment and settings loading"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

