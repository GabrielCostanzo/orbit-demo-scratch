[project]
name = "meridian-telemetry"
version = "0.1.0"
description = "Internal tracing helpers"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

